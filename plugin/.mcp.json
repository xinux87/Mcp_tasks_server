{
  "mcpServers": {
    "tareas": {
      "type": "http",
      "url": "${user_config.servidor_url}/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.token_terminal}"
      }
    }
  }
}
