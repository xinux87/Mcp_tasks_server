{
  "description": "Al arrancar la sesion copia el script de statusline a ~/.claude/mcp-tareas/statusline.sh, que es su ruta estable, y vuelca la URL del servidor y el token del terminal a ~/.claude/mcp-tareas/config para que ese script pueda leerlos.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/guardar-config.sh",
            "async": true,
            "timeout": 10
          }
        ]
      }
    ]
  }
}
