{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "mcp-tareas",
  "displayName": "MCP Tareas",
  "version": "0.2.0",
  "description": "Reporta a la web el uso de la cuenta de este terminal desde la linea de estado de Claude Code. Opcional: el servidor MCP de tareas y la skill del bucle se conectan con dos comandos, sin plugin.",
  "author": {
    "name": "xinux87"
  },
  "homepage": "https://github.com/xinux87/Mcp_tasks_server",
  "repository": "https://github.com/xinux87/Mcp_tasks_server",
  "keywords": [
    "tareas",
    "mcp",
    "statusline",
    "uso",
    "cuenta"
  ],
  "license": "MIT",
  "mcpServers": "./.mcp.json",
  "userConfig": {
    "servidor_url": {
      "type": "string",
      "title": "URL del servidor",
      "description": "URL base del servidor MCP de tareas, sin barra final. Ejemplo: https://tareas.example.com",
      "required": true
    },
    "token_terminal": {
      "type": "string",
      "title": "Token del terminal",
      "description": "Token que la web entrega una sola vez al crear el terminal. Identifica a este terminal y a su usuario.",
      "sensitive": true,
      "required": true
    }
  }
}
